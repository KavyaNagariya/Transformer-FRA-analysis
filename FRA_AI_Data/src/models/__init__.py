"""
ML training, prediction, and statistical anomaly helpers.

Import from submodules explicitly, e.g. ``from src.models.predict import predict_fault``,
to avoid loading optional training dependencies when only statistics are needed.
"""
